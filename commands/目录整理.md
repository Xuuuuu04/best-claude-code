---
description: 轻量级项目目录治理：识别冗余文件、重复目录、命名不规范、孤儿文件，产出整理方案并执行。不触碰业务代码，只整理结构。触发信号："目录太乱"、"整理一下目录"、"清理冗余文件"、"文件命名不规范"。
argument-hint: [可选：指定目录路径，默认当前工作目录]
---

<command>

<section id="rules">
<title>铁律</title>
<content>
1. **No business code changes**: This command ONLY touches file organization — moves, renames, and deletions of non-code structural files. Any change that alters import paths, breaks builds, or modifies logic MUST be stopped and escalated to dev-lead.
2. **User approval before any move/delete**: The dispatch flow MUST pause after producing the reorganization plan and wait for explicit user approval before any file operations are executed.
3. **Backup or git-clean precondition**: Before executing any move or delete, verify that `git status` is clean or that the user has acknowledged backup responsibility. No irreversible file operations without this check.
4. **Scope limited to structural files**: Directories like `node_modules` / `.git` / `dist` / `build` / `__pycache__` are excluded from analysis. Config files that affect build behavior require dev-lead sign-off.
5. **STOP if import paths break**: If moving a file would require updating import paths in business code, STOP — escalate to dev-lead to handle it as part of /新功能 or /快速修复.
</content>
</section>

<section id="scope">
<title>本 command 覆盖范围</title>
<content>
**整理范围**
- 冗余文件识别（重复内容 / 空文件 / 临时文件如 `*.tmp` / `*.bak` / `test-*.log`）
- 重复目录识别（相同职责的目录存在多个副本）
- 命名不规范（大小写混用 / 空格 / 中文路径 / 不一致的命名风格）
- 孤儿文件（不被任何代码引用且不属于文档/配置的文件）
- 过深嵌套（超过 4 层无业务价值的目录嵌套）

**不触碰范围**
- 业务代码文件的内容
- 会影响 import 路径的移动（需要 dev-lead）
- `.env` / 密钥文件 / 安全敏感配置
- 用户明确标注"不动"的目录
</content>
</section>

<section id="dispatch-flow">
<title>调度流水线</title>
<content>
**Step 1 — 扫描分析**（派 技术调研师 Agent）
- 扫描目标目录树（排除 node_modules / .git / dist / build / __pycache__）
- 识别问题清单：冗余文件 / 重复目录 / 命名不规范 / 孤儿文件 / 过深嵌套
- 产出问题报告，按严重度分级（高：影响开发体验 / 中：命名不规范 / 低：历史遗留）

**Step 2 — 整理方案设计**（派 开发组长 Agent）
- 基于 Step 1 问题报告，产出具体整理方案
- 每项操作明确：操作类型（移动/重命名/删除）+ 原路径 + 目标路径 + 理由
- 评估每项操作是否影响 import 路径（若影响 → 标记为"需 dev-lead 额外处理"）
- 产出整理方案文件：`docs/directory-cleanup-plan.md`

**Step 3 — 用户拍板（强制停顿）**
- 主进程呈现整理方案摘要（影响文件数 / 操作类型分布）
- **必须等待用户明确确认才进入 Step 4**
- 用户可要求排除某些项目或调整优先级

**Step 4 — 执行整理**（根据方案派 后端开发师 或 frontend）
- 按 Step 2 批准方案执行：移动 / 重命名 / 删除（仅安全操作）
- 每步操作后输出结果日志
- 遇到任何影响 import 路径的操作 → 立即 STOP，记录并汇报

**Step 5 — 验证无破坏**（派 代码审计师 Agent）
- 验证整理后项目构建不破坏（`npm run build` / `cargo build` / `go build` 等）
- 验证无意外删除（Git diff 确认改动范围符合预期）
- 产出验证报告
</content>
</section>

<section id="output-contract">
<title>产出合约</title>
<content>
```
## 目录整理报告

**扫描范围**：[目录路径]
**发现问题**：[N 个，按高/中/低分级]
**方案概要**：[移动 X 个 / 重命名 Y 个 / 删除 Z 个]
**整理方案文件**：[docs/directory-cleanup-plan.md]
**用户确认状态**：[等待确认 / 已确认]
**执行结果**：[成功 N / 跳过 M（原因：影响 import）]
**构建验证**：[通过 / 失败（详情）]
**后续建议**：[需 dev-lead 处理的 import 更新项]
```
</content>
</section>

<section id="final_reminder">
<title>Final Reminder</title>
<content>
- Never execute file operations without explicit user approval after Step 2 plan delivery.
- Any move that breaks import paths is OUT of scope — hand it to dev-lead.
- Verify git status before destructive operations (rename / delete).
- Business code content is never touched — only structural organization.
- 代码审计师 validates no build breakage before declaring done.
</content>
</section>

</command>
